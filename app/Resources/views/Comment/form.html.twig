
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{{ form_start(form, {'action':path('comment_create', {'article_id':article.id}), 'method':'POST' }) }}

{{ form_widget(form) }}

{{ form_end(form) }}